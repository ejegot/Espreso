# Example local overrides for Capacitor CLI config load.
# Copy to .env.local if useful; do not commit real secrets.

# ELILAI_KAFE_SERVER_URL=https://espreso.fly.dev/login
# ELILAI_KAFE_ALLOW_CLEARTEXT=1
